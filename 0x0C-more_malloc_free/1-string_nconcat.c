#include "main.h"
#include <stdlib.h>
/**
* string_nconcat - fun
* @b: value
*
* Return: ptr
*/
char *string_nconcat(char *s1, char *s2, unsigned int n);
{
	void *ptr = malloc(b);
	if (!ptr)
		exit(98);
	return (ptr);
}
